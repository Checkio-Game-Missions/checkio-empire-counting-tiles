def tiles(radius):
    return 0, 0


if __name__ == '__main__':
    # These "asserts" using only for self-checking and not necessary for auto-testing
    assert tiles(2) == [4, 12], "N=2"
    assert tiles(3) == [16, 20], "N=3"
    assert tiles(2.1) == [4, 20], "N=2.1"
    assert tiles(2.5) == [12, 20], "N=2.5"
